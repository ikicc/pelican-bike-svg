# Pelican Bike SVG

一个纯 HTML + SVG + CSS 实现的 2D 循环动画：鹈鹕骑着自行车前进。

## 特点

- 单文件即可运行
- SVG 绘制鹈鹕、自行车、道路与背景
- CSS 动画控制车轮、踏板、腿部、翅膀、头部、云朵和道路运动
- 无外部图片、字体或 JavaScript 依赖
- 支持 `prefers-reduced-motion`

## 使用

直接用浏览器打开 `index.html` 即可。

也可以把仓库部署到 GitHub Pages，入口文件就是 `index.html`。
